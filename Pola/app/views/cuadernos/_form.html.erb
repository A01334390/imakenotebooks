<%= form_for @cuaderno, :html => { :class => "form-horizontal cuaderno" } do |f| %>

  <% if @cuaderno.errors.any? %>
    <div id="error_expl" class="panel panel-danger">
      <div class="panel-heading">
        <h3 class="panel-title"><%= pluralize(@cuaderno.errors.count, "error") %> prohibited this cuaderno from being saved:</h3>
      </div>
      <div class="panel-body">
        <ul>
        <% @cuaderno.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
        </ul>
      </div>
    </div>
  <% end %>

  <div class="form-group">
    <%= f.label :tiempo, :class => 'control-label col-lg-2' %>
    <div class="col-lg-10">
      <%= f.text_field :tiempo, :class => 'form-control' %>
    </div>
    <%=f.error_span(:tiempo) %>
  </div>
  <div class="form-group">
    <%= f.label :tipo, :class => 'control-label col-lg-2' %>
    <div class="col-lg-10">
      <%= f.text_field :tipo, :class => 'form-control' %>
    </div>
    <%=f.error_span(:tipo) %>
  </div>
  <div class="form-group">
    <%= f.label :beneficio, :class => 'control-label col-lg-2' %>
    <div class="col-lg-10">
      <%= f.text_field :beneficio, :class => 'form-control' %>
    </div>
    <%=f.error_span(:beneficio) %>
  </div>

  <div class="form-group">
    <div class="col-lg-offset-2 col-lg-10">
      <%= f.submit nil, :class => 'btn btn-primary' %>
      <%= link_to t('.cancel', :default => t("helpers.links.cancel")),
                cuadernos_path, :class => 'btn btn-default' %>
    </div>
  </div>

<% end %>
